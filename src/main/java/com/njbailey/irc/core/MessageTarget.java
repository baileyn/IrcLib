package com.njbailey.irc.core;

    /**
     * Return the name of the message's target.
     * @return the name of the message's target.
     */
public interface MessageTarget {
    String getName();
}